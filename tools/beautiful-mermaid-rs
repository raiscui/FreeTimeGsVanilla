#!/usr/bin/env bash
set -euo pipefail

# 说明:
# - 这是一个薄封装,把命令名固定为 "beautiful-mermaid-rs".
# - 实际实现位于 tools/mermaid/beautiful-mermaid-rs.mjs.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
node "${SCRIPT_DIR}/mermaid/beautiful-mermaid-rs.mjs" "$@"

